"""fairscape-improve: a single-file web form that helps a crate author fill in
the root-entity (and Software-entity) properties the AI-Ready graders read.

v0.1 scope: set or edit single properties on entities that already exist in
``ro-crate-metadata.json``. No new entities, no new graph links.
"""
